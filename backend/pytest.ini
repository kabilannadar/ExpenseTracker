[pytest]
testpaths = tests
asyncio_mode = auto
# Show short traceback, verbose test names
addopts = -v --tb=short
# Suppress noisy logs from third-party libraries during tests
log_cli = true
log_cli_level = WARNING
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
